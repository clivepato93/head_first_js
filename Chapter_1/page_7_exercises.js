var price = 28.99;
// price = 28.99;

var discount = 10;
// discount = 10;

var total = price - (price * (discount / 100));
// total = 25.2

if (total > 25) {
freeShipping(); 
}
// freeShipping function is executed
var count = 10;
while (count > 0) {
juggle();
count = count - 1;
}
// juggle is executed 9 times

var dog = {name: "Rover", weight: 35};
if (dog.weight > 30) {
alert("WOOF WOOF");
} else {
alert("woof woof");
}
alert("WOOF WOOF");

var circleRadius = 20;
var circleArea =
Math.PI * (circleRadius * circleRadius);
// 1256

